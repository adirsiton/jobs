import { createStyles } from '@material-ui/core';

const styles = () => createStyles({
    starIconYellow:{
        color: 'yellow'
    },
    starIconWhite: {
        color: 'white'
    },
    favoriteTitle: {
        fontWeight: "bold",
        fontSize: "24px",
        marginBottom: "10px",
        color: "#595959"
    }
});

export default styles;